import { JsonHighlighterPipe } from './json-highlighter-pipe';

describe('JsonHighlighterPipe', () => {
  it('create an instance', () => {
    const pipe = new JsonHighlighterPipe();
    expect(pipe).toBeTruthy();
  });
});
